language: node_js
node_js:
  - "9"

cache:
  yarn: true
  directories:
    - "node_modules"
    - ".cache"

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version v1.6.0
  - export PATH="$HOME/.yarn/bin:$PATH"

script: 
- yarn test:ci